<!DOCTYPE html>
<html>
<head>
  <title><%= Rails.application.config.system_name %></title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

	<div class="container-fluid">
		<div class="row">
			<div class="col-md-10 col-md-offset-1">

					<% unless notice.nil? %>
    				<div class="row">
		  				<p class="col-md-8 col-md-offset-2 alert alert-warning marin-top-1"><%= notice %></p>
            </div>
					<% end %>

					<% unless alert.nil? %>
				    <div class="row">
						  <p class="col-md-8 col-md-offset-2 alert alert-danger margin-top-1"><%= alert %></p>
            </div>
					<% end %>

				<div class="row">
					<%= yield %>
				</div>
			</div>
		</div>
	</div>
	
</body>
</html>
